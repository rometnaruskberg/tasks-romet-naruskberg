const colors = ['Red', 'Green', 'White', 'Black'];
const dot = colors.join('.');
console.log(dot)

const comma = colors.join(', ');
console.log(comma)

const plus = colors.join(' + ');
console.log(plus)